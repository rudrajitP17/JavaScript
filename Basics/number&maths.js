//ways of initialising numbers
const k=400

const k1=new Number(500) // this again stores the value in key value and provides function to you

console.log(k)
console.log(k1)
console.log(k1.toString.length)
console.log(k1.toFixed(2)) // After decimal you can fix upto two decimal places

const hund=1000000000
console.log(hund.toLocaleString('en-IN')) // it returns values with commas in us local system, to
//specify Indian use 'en-IN'

console.log(Math)
console.log(Math.ceil(4.2))
console.log(Math.floor(4.9))
console.log(Math.random()) // Always returns a value between 1 and 0
